import React from 'react';

import '../../sass/components/Footer.scss';

const Footer = () => {
    return (
        <div className="footer">
            this is footer
        </div>
    );
};

export default Footer;